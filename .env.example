# Copy this file to .env.local and fill in your own values.
# .env.local is ignored by git. Never commit real keys.

# Server-side only. The browser never sees this key; it only gets short-lived tokens.
ASSEMBLYAI_API_KEY=
